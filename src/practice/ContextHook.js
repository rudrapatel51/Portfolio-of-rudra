import { createContext } from "react";

export const counterContext = createContext(0)
// this hook is been used to pass the value to all component who need the value 
//and it easy to pass one coponent rather then pass to different components